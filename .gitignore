# clasp (Apps Script CLI), if used later
.clasp.json
.clasprc.json

# OS / editor
.DS_Store
*.swp

# order-link-proxy (Python)
order-link-proxy/.env
order-link-proxy/__pycache__/
order-link-proxy/.venv/
